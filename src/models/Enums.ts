export enum MediaType{
  mp3 = 1,
  mp4 = 2
}

export enum MenuItem {
  search = 1,
  history = 2
}

export enum SearchType{
  videos = 1,
  playlists = 2
}

export enum PlaylistVideoResultStatus{
  notMarked = 1,
  marked = 2,
  downloaded = 3,
  error = 4
}
